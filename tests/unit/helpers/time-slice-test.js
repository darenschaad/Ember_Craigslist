import { timeSlice } from 'craigslist/helpers/time-slice';
import { module, test } from 'qunit';

module('Unit | Helper | time slice');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timeSlice([42]);
  assert.ok(result);
});
